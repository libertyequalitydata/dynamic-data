import { album, artist, artistRelatedArtists, artistTopTracks, shows, playlist, track, trackAudioAnalysis, trackAudioFeatures, userProfile, episode } from "./mockups";


  const Data = {
    album,
    artist,
    artistRelatedArtists,
    artistTopTracks,
    shows,
    playlist,
    track,
    trackAudioAnalysis,
    trackAudioFeatures,
    userProfile,
    episode
  };
  
  export default Data;

  