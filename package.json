{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "faker": "^5.5.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run --parallel test"
  }
}
