{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "build": "yarn install && lerna run build",
    "test": "lerna run test --parallel --no-sort"
  }
}
